apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: airflow
  namespace: airflow
spec:
  interval: 5m
  chart:
    spec:
      chart: airflow
      version: 19.6.0  # Check latest version at https://artifacthub.io/packages/helm/bitnami/airflow
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    defaultAirflowRepository: apache/airflow
    defaultAirflowTag: 2.9.1
    airflow:
      executor: LocalExecutor
    postgresql:
      enabled: true
    redis:
      enabled: true
